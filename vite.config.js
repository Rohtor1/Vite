import { defineConfig } from 'vite';

export default defineConfig({
  base: '/Vite/', // Replace "your-repo-name" with the actual repository name
  // You can add other configurations here if needed
});